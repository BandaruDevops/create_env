replicaCount: 1

image:
  repository: bandarudocker/myapp
  tag: main
  pullPolicy: Always

service:
  type: LoadBalancer
  port: 3000

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"
httpRoute:
  enabled: false
ingress:
  enabled: false
